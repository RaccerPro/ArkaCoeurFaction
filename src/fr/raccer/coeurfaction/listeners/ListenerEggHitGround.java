package fr.raccer.coeurfaction.listeners;

import org.bukkit.event.Listener;

public class ListenerEggHitGround implements Listener {

	/*
	@EventHandler
	public void onProjectile(ProjectileHitEvent e) {
		
		if(e.getEntityType() != EntityType.EGG) return ;
		
		Entity ent = e.getEntity() ;
		
		if(!ent.hasMetadata("egg_not_spawn_chicken")) return ;
		ent.remove();
	}
	*/
	
}
